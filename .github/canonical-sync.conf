# <path-in-this-repo> <path-in-jlapenna/agent-lcars>
#
# Byte-identical copies of files agent-lcars owns (agent-lcars#1307).
# Cross-repository source imports are forbidden fleet-wide, so these are
# copies rather than a shared dependency; this manifest keeps them honest.
# CI runs jlapenna/agent-lcars/.github/actions/check-canonical-sync@main
# with --forbid-strays against it (see .github/workflows/validate.yml).
#
# Repo-specific behavior enters only through each script's documented
# hook (require-feature-worktree.sh's $1 / REQUIRE_WORKTREE_EXTRA_HINT,
# the AGENT_FLEET_LOGIN env override) -- never by editing a copy.
tools/codex-issue-guardrail.cjs tools/codex-issue-guardrail.cjs
tools/fleet-identity.cjs tools/fleet-identity.cjs
tools/claude-agent-session.sh tools/claude-agent-session.sh
tools/require-feature-worktree.sh tools/require-feature-worktree.sh
.agents/skills/github-ci-monitor/scripts/watch-prs.sh .agents/skills/github-ci-monitor/scripts/watch-prs.sh
