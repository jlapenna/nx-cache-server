{
  "$schema": "https://opencode.ai/config.json",
  "model": "homelab/default",
  "permission": {
    "external_directory": "allow",
    "doom_loop": "allow"
  },
  "compaction": {
    "reserved": 3000,
    "prune": true
  },
  "provider": {
    "homelab": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "Homelab LiteLLM (spark)",
      "options": {
        "baseURL": "https://llm.lan.jlapenna.net/v1",
        "apiKey": "{env:OPENCODE_LLM_API_KEY}"
      },
      "models": {
        "default": {
          "name": "Spark Default (DeepSeek-V4-Flash, pinned production route). context 60000, not the backend's 262144: OpenCode uses this number only to decide when to compact -- see jlapenna/agent-lcars docs/opencode-context-limit.md before changing it.",
          "limit": {
            "context": 60000,
            "output": 8192
          }
        },
        "default-nothink": {
          "name": "Spark Default with thinking disabled (same backend as default; LiteLLM forces thinking:{type:disabled} via extra_body). Same context rationale as default.",
          "limit": {
            "context": 60000,
            "output": 8192
          }
        }
      }
    }
  }
}
